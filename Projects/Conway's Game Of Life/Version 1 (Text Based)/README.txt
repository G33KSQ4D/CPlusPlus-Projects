Created March 26th, 2018 as a way for me to learn some C++ and have fun at the same time

This is the first version and it only uses text graphics and no objects.

The second version will use objects in order to make SFML (Graphics) easier for modelling. It will also had some organization
